- name: Ejecutar automatización vía API
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Llamar API interna
      uri:
        url: "http://10.100.32.114:9090/token"
        method: POST
        body_format: json
        body:
          automation: "prueba endpoint token"
        return_content: true
      register: resultado

    - debug:
        var: resultado.json
